import { Metadata } from 'next';
import { FC } from 'react';

export const metadata: Metadata = {
  title: 'About us'
};

const AboutPage: FC = () => {
  return <h3>Select some page</h3>;
};

export default AboutPage;
